# Rust
/target/
**/target/
*.pdb

# Frontend
**/node_modules/
**/dist/

# Tauri
/src-tauri/target/
/src-tauri/gen/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Logs / runtime
*.log
*.log.*
logs/

# 临时 / 工作区
.scratch/
.claude/
.loop/

# Secrets
*.env
*.env.local
.env
release.env.local

# Tauri updater 签名私钥（绝不可进 git；公钥嵌在 tauri.conf.json，.pub 文件本地保留即可）
naxone-updater.key
*.key
*.key.pub

# Tauri 构建产物会放在 target/ 下，上面已忽略。
# 但独立的 bundle 目录也防一手：
/bundle/
/AppImage/

# playwright-mcp 临时缓存（CDP 会话快照、控制台日志等，不入仓库）
.playwright-mcp/
