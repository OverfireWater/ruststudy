# NaxOne v0.5.27

本版优化窗口动效与后台运行时的 GPU 消耗，同时保留前台使用时的视觉效果。

### GPU 与动效优化

- 窗口处于前台、可见且获得焦点时，继续展示页面动效。
- 窗口失焦、隐藏、最小化或页面不可见时，暂停 CSS 动画和过渡效果。
- 应用进入后台时停止 Dashboard 的轮询任务，回到前台后自动恢复。
- 降低背景模糊半径与背景动画渲染范围，减少 WebView2 合成压力。
- 减少 Dashboard 日志刷新频率，并避免无变化时重复触发列表渲染。
